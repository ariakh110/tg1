## 1. Frontend (kavehmetal)
- [x] 1.1 `getGroupGrade(product)` returns the grade label/value (fallback «سایر گریدها»)
- [x] 1.2 `groupProducts` groups by grade instead of factory; section title shows the grade
- [x] 1.3 `npx eslint`; `npm run build` compiles

## 2. Verification
- [ ] 2.1 After deploy: `/products` shows separate grade sections (ST52, ST37, CK45…), each titled by grade; factory still shown per row
